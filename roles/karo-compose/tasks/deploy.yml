# SPDX-FileCopyrightText: 2025 hazzuk
#
# SPDX-License-Identifier: AGPL-3.0-only

---

- name: "Create compose stack directory for {{ stack.name }}"
  ansible.builtin.file:
    path: "/srv/docker/{{ stack.path }}"
    mode: "0774"
    state: directory

- name: "Deploy compose templates for {{ stack.name }}"
  ansible.builtin.template:
    src: "{{ template.src }}"
    dest: "/srv/docker/{{ stack.path }}/{{ template.path | splitext | first }}"
    mode: "0644"
  with_community.general.filetree: ../templates/{{ stack.path }}/
  loop_control:
    loop_var: template
    label: "{{ template.path }}"
